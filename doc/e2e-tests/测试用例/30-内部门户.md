# 30 · 内部门户

> 对应代码：[tests/smoke-internal/](../tests/smoke-internal/) · 账号 `li.reader`（内部查阅者）

## 工作台 /internal/overview — overview.spec.ts
| 编号 | 场景 | 期望 |
|---|---|---|
| I-OV-1 | 工作台正常加载 | 不卡在「加载工作台…」（回归人#4） |
| I-OV-2 | 两核心区块 | 「我的最近查阅」「我的借阅申请」可见 |
| I-OV-3 | 无 ISO 时间 | 整页无未格式化 ISO 时间 |

## 检索 /internal/search — search.spec.ts
| 编号 | 场景 | 期望 |
|---|---|---|
| I-SEARCH-1 | 确认检索出结果 | 点「确认检索」→ 出「共 N 条」+ 数据行 |
| I-SEARCH-2 | 标签 + 利用状态列（R3-C1） | 表头含「标签」「利用状态」 |
| I-SEARCH-3 | 分页器 | `.el-pagination` 可见 |
| I-SEARCH-4 | 详情弹窗 | 点详情 → el-dialog「档案详情」 |

> 注意：本页 onMounted 不自动检索（只加载字典），需点「确认检索」。结果计数「共 N 条」与分页器 total 同时出现，断言用 `.first()`。

## 借阅申请 /internal/borrow-requests — borrow-requests.spec.ts
| 编号 | 场景 | 期望 |
|---|---|---|
| I-BR-1 | 列表表头齐全 | 申请号/档案题名/申请时间/状态/操作 |
| I-BR-2 | 发起新申请入口 | 链接存在且跳 /internal/search |

## 覆盖回归点
人#4 工作台卡加载、R3-C1 标签/利用状态列、B7-5 借阅字段扁平化。
