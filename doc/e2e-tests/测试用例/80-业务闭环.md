# 80 · 业务闭环（阶段二）

> 跨角色业务闭环 E2E，复用阶段一的 support（账号/鉴权/夹具/体感断言）+ 阶段二的 [workflow-state](../support/workflow-state.ts)（跨步骤共享编号）/[ai](../support/ai.ts)（异步轮询）。
> 目录 [tests/workflows/](../tests/workflows/)，每闭环一个 spec，`serial` 链式依赖。

## 进展

| 闭环 | spec | 角色 | 状态 | 说明 |
|---|---|---|---|---|
| **W-G 数据研判** | w-g-research | liu.back | ✅ 绿 | 扫描（真实 AI）→ 轮询完成 → 异常项 → 候选四要素 → 删除（R3-C2） |
| **W-F 档案编研** | w-f-compilation | liu.back | ✅ 绿 | 新建 → 加素材 → 一键「生成正文并入库」（D3）→ 产出 ARC |
| **W-A 公众捐赠** | w-a-donation | 小周→小刘→小陈→小刘 | ✅ 绿（4 步） | 提交 BAT → 联系约定 → 前台真实上传+ClamAV 匹配 → AI 补全+入库产出 ARC |
| W-B 移交入库 | — | 小张→小陈→小刘→小李 | ⏳ 待做 | |
| W-C 密级/开放调整 | — | 小刘→小方 | ⏳ 待做 | |
| W-D 鉴定→销毁 | — | 小刘→小方→小刘 | ⏳ 待做 | |
| W-E 借阅 | — | 小李→小刘→小陈→小李 | ⏳ 待做 | |

## 已验证的跨角色模式
- `serial` + 多 `describe`（每角色一个 `storageState`）+ `wf` 跨步骤传递 BAT/ARC 号。
- 公众拖文件只解析名 → 前台 `#collectionFilePicker` 真实上传 → 后端按文件名匹配 + ClamAV 扫描。
- 状态机 pending_contact→pending_receive→received→archived(normal) 全链路走通，产出正式 ARC。

## 调研澄清：先前标注的「两个产品问题」实际后端均正常
1. **aiStatus 未回写**：curl 实测后端 detail `latestAiTaskStatus` 回填**正常**（completed 批次正确），前端 normalize（[archive.ts:49-52](../../frontend/src/api/archive.ts#L49)、[archive.ts:109-114](../../frontend/src/api/archive.ts#L109)）也映射了——**非后端 bug**（先前基于漏 `.data` 的 jq + 特例批次的误判）。
2. **入库无 ARC**：curl 手动 confirmItem + archiveItem 成功产出 **ARC-000045**，handleArchive 逻辑正常——**非后端 bug**。

## ✅ 调研中发现的真问题（已修）
- **handleRunAi 假阳性**（前端 pending-archive，A 类）：[index.vue:399-407](../../frontend/src/views/admin/pending-archive/index.vue#L399) 轮询 getAiTask 30 次后**无论任务是否完成都弹「AI 整批补全完成」**，误导用户/测试以为补全完成、后续按缺失字段入库。已修：超时改弹「AI 补全仍在进行，请稍后刷新」并 return，不再假报完成。

## 实现要点（适用后续闭环）
- 跨角色状态：`wf('W-X').batchNo/archiveNo/...`，同 spec serial 内共享。
- AI 异步：前端多不自动轮询，需主动点任务项刷新或用完成提示判断；handleRunAi 现已区分真完成/超时。
- 真实上传：`setInputFiles(FIXTURES.xxx)`，后端真实 ClamAV。
- 表单字段容器：pending-archive 用 `.detail-field`。
