# 借阅跨单位代章 + 库房/全宗删除 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: 用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 按任务逐步实施。步骤用 `- [ ]` 复选框跟踪。

**Goal:** 补齐三处业务缺口——内部人员借阅跨单位纸质档案由档案馆代章（含检索分级）、库房物理删除、全宗物理删除。

**Architecture:** 后端 Java 17 + Spring Boot 3 + MyBatis-Plus（PostgreSQL）。前端 Vue 3 + TS + Element Plus。后端用纯 Mockito 单测（不开 Spring、不连库）先写失败测试再实现；前端改动用浏览器实测 + e2e 回归。三个需求相互独立，按风险从低到高分 4 个 Phase，每个 Phase 独立 commit、可独立验证。

**Tech Stack:** JUnit 5 / AssertJ / Mockito；MyBatis-Plus QueryWrapper；Playwright e2e（`doc/e2e-tests/`）。

**配套 spec:** [doc/需求调整-借阅跨单位代章与库房全宗删除-设计.md](需求调整-借阅跨单位代章与库房全宗删除-设计.md)

**通用约定（所有后端任务适用）**
- 单测位置：`backend/src/test/java/com/archive/service/`，沿用 `OrganizationServiceTest` 范式——`@BeforeEach` 手动 `mock(...)` 每个依赖并 `new XxxService(...)` 注入；断言用 AssertJ `assertThat` / `assertThatThrownBy`。
- 跑单测：`cd backend && mvn test -Dtest=<TestClass>`（若项目带 maven wrapper 改用 `./mvnw`）。
- 审计：`auditService.log(模块码, 动作, 实体类型, id, Map)`。全宗=M06、库房=M07、借阅=M09。
- 错误码：`ErrorCode.NOT_FOUND`(404)、`BUSINESS_CONFLICT`(409)、`FORBIDDEN`(403)；`throw new BusinessException(ErrorCode, msg)`。
- `R.ok()` / `R.ok(data)` 均可用（见 `common/R.java`）。
- 每个 Phase 末尾 commit，提交信息用 `type(scope): 简体中文`（见 `doc/commit-convention.md`）。当前分支 `feat/integration-fang`，git 身份 `fangjiangsu`。

**涉及文件总览**

| Phase | 后端 | 前端 |
|-------|------|------|
| 1 全宗删除 | `FondsService.java`、`FondsController.java`、`FondsServiceTest.java` | `api/fonds.ts`、`views/admin/fonds/index.vue` |
| 2 库房删除 | `WarehouseService.java`、`WarehouseController.java`、`WarehouseServiceTest.java` | `api/warehouse.ts`、`warehouse/components/RoomList.vue`、`warehouse/index.vue` |
| 3 凭证代章 | `PdfGenerator.java`、`BorrowService.java`（+ `BorrowServiceTest.java` 调整） | — |
| 4 检索分级 | `SearchService.java`、`SearchServiceTest.java` | —（e2e 回归） |
| 5 文档同步 | — | `doc/业务场景.md`、`doc/接口文档.md` |

---

## Phase 1：全宗物理删除（关联 fonds_id 置空）

### Task 1.1：`FondsService.deleteFonds`（TDD）

**Files:**
- Modify: `backend/src/main/java/com/archive/service/FondsService.java`（在 `updateFonds` 方法后追加 `deleteFonds`）
- Test: `backend/src/test/java/com/archive/service/FondsServiceTest.java`（追加 3 个测试方法）

> 前置：`FondsServiceTest` 现有 `@BeforeEach` 已 mock `fondsMapper / organizationMapper / auditService / jdbcTemplate` 并构造 `service`（与 `updateFonds`/`countAssoc` 测试共用）。若实际变量名不同，按文件内现有命名对齐。

- [ ] **Step 1: 写失败测试**（追加到 `FondsServiceTest.java`，import 补 `org.junit.jupiter.api.Test`、`org.assertj.core.api.Assertions.assertThatThrownBy`、`static org.mockito.ArgumentMatchers.*`、`static org.mockito.Mockito.*`、`com.archive.exception.BusinessException`、`com.archive.common.ErrorCode`）

```java
@Test
void deleteFonds_无关联_置空引用后物理删除() {
    Fonds fonds = new Fonds();
    fonds.setId(5L);
    fonds.setFondsName("误建全宗");
    when(fondsMapper.selectById(5L)).thenReturn(fonds);
    when(jdbcTemplate.queryForObject(eq("SELECT COUNT(*) FROM archives WHERE fonds_id = ?"),
            eq(Long.class), eq(5L))).thenReturn(0L);
    when(jdbcTemplate.queryForObject(eq("SELECT COUNT(*) FROM archive_boxes WHERE fonds_id = ?"),
            eq(Long.class), eq(5L))).thenReturn(0L);

    service.deleteFonds(5L);

    verify(jdbcTemplate).update(eq("UPDATE archives SET fonds_id = NULL WHERE fonds_id = ?"), eq(5L));
    verify(jdbcTemplate).update(eq("UPDATE archive_boxes SET fonds_id = NULL WHERE fonds_id = ?"), eq(5L));
    verify(fondsMapper).deleteById(5L);
    verify(auditService).log(eq("M06"), eq("delete_fonds"), eq("fonds"), eq(5L), any());
}

@Test
void deleteFonds_有关联_仍置空引用后删除() {
    Fonds fonds = new Fonds();
    fonds.setId(6L);
    fonds.setFondsName("在用全宗");
    when(fondsMapper.selectById(6L)).thenReturn(fonds);
    when(jdbcTemplate.queryForObject(eq("SELECT COUNT(*) FROM archives WHERE fonds_id = ?"),
            eq(Long.class), eq(6L))).thenReturn(3L);
    when(jdbcTemplate.queryForObject(eq("SELECT COUNT(*) FROM archive_boxes WHERE fonds_id = ?"),
            eq(Long.class), eq(6L))).thenReturn(2L);

    service.deleteFonds(6L);

    verify(jdbcTemplate).update(eq("UPDATE archives SET fonds_id = NULL WHERE fonds_id = ?"), eq(6L));
    verify(jdbcTemplate).update(eq("UPDATE archive_boxes SET fonds_id = NULL WHERE fonds_id = ?"), eq(6L));
    verify(fondsMapper).deleteById(6L);
}

@Test
void deleteFonds_全宗不存在_抛NOT_FOUND() {
    when(fondsMapper.selectById(99L)).thenReturn(null);
    assertThatThrownBy(() -> service.deleteFonds(99L))
            .isInstanceOf(BusinessException.class)
            .extracting("errorCode").isEqualTo(ErrorCode.NOT_FOUND);
}
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd backend && mvn test -Dtest=FondsServiceTest`
Expected: 编译失败——`deleteFonds` 方法不存在。

- [ ] **Step 3: 实现 `deleteFonds`**（追加到 `FondsService.java`，紧跟 `updateFonds` 之后。所需 import `BusinessException`/`ErrorCode`/`Map` 均已在文件内）

```java
/** 17.4 删除全宗（物理删除；关联档案/档案盒的 fonds_id 先置空）。 */
@Transactional
public void deleteFonds(Long id) {
    Fonds fonds = fondsMapper.selectById(id);
    if (fonds == null || fonds.getDeletedAt() != null) {
        throw new BusinessException(ErrorCode.NOT_FOUND, "全宗不存在");
    }
    long assoc = countAssoc(id);
    // 外键 RESTRICT：必须先解除 archives / archive_boxes 对全宗的引用，再物理删除
    jdbcTemplate.update("UPDATE archives SET fonds_id = NULL WHERE fonds_id = ?", id);
    jdbcTemplate.update("UPDATE archive_boxes SET fonds_id = NULL WHERE fonds_id = ?", id);
    fondsMapper.deleteById(id);
    auditService.log("M06", "delete_fonds", "fonds", id,
            Map.of("fondsName", fonds.getFondsName(), "assoc", assoc));
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd backend && mvn test -Dtest=FondsServiceTest`
Expected: PASS（3 个新用例 + 原有用例全绿）。

### Task 1.2：`FondsController` DELETE 端点

**Files:**
- Modify: `backend/src/main/java/com/archive/controller/FondsController.java`（追加 `delete` 方法，`R`/`DeleteMapping`/`requireBackOrAdmin` 已在文件内）

- [ ] **Step 1: 追加端点**（在 `update` 方法后）

```java
@DeleteMapping("/{fondsId}")
public R<Void> delete(@PathVariable Long fondsId) {
    requireBackOrAdmin();
    fondsService.deleteFonds(fondsId);
    return R.ok();
}
```

- [ ] **Step 2: 编译校验**

Run: `cd backend && mvn -q compile`
Expected: 编译通过，无报错。

### Task 1.3：前端全宗删除按钮

**Files:**
- Modify: `frontend/src/api/fonds.ts`（`removeFonds` 已存在，无需改）
- Modify: `frontend/src/views/admin/fonds/index.vue`（import、操作列按钮、新增 `onDelete` 函数、更新 `removeOrDisable` 注释）

- [ ] **Step 1: import 增补 `ElMessageBox` 与 `removeFonds`**（替换 `index.vue:151-152` 两行）

```ts
import { ElMessage, ElMessageBox } from 'element-plus'
import { createFonds, getFonds, removeFonds, updateFonds } from '@/api/fonds'
```

- [ ] **Step 2: 操作列加「删除」按钮**（在 `index.vue:73` 停用按钮后追加一行）

```html
                      <el-button size="small" type="danger" @click="onDelete(f)">删除</el-button>
```

- [ ] **Step 3: 新增 `onDelete` 函数**（紧跟 `removeOrDisable` 之后，约 `index.vue:295`）

```ts
async function onDelete(f: FondsItem) {
  const linked = isLinked(f)
  const tip = linked
    ? `该全宗关联 ${f.archiveCount} 件档案 / ${f.boxCount} 个档案盒，删除后这些记录的全宗字段将被清空，且不可恢复。确认删除？`
    : '删除全宗不可恢复，确认删除？'
  try {
    await ElMessageBox.confirm(tip, '删除全宗', {
      type: 'warning', confirmButtonText: '删除', cancelButtonText: '取消',
    })
  } catch {
    return // 用户取消
  }
  try {
    await removeFonds(f.id)
    ElMessage.success('全宗已删除')
    if (selectedId.value === f.id) selectedId.value = null
    await loadAll()
  } catch (e) {
    ElMessage.error((e as Error).message || '删除失败')
  }
}
```

- [ ] **Step 4: 更新 `removeOrDisable` 上方注释**（`index.vue:288`，反映删除已支持）

将原注释：
```ts
    // 接口文档 §17.4 明确全宗不做物理删除；无论是否有关联数据，统一通过 status=disabled 停用，保留历史档案归属
```
改为：
```ts
    // 停用：仅置 status=disabled，保留全宗与历史归属；如需彻底清理误建全宗，使用「删除」（onDelete，关联 fonds_id 置空）
```

### Task 1.4：浏览器验证 + commit

- [ ] **Step 1: 启动前后端**（后台运行；后端在 8080、前端按项目默认）

Run（后端）: `cd backend && mvn spring-boot:run`（后台）
Run（前端）: `cd frontend && npm run dev`（后台）

- [ ] **Step 2: 浏览器验证全宗删除闭环**（用 admin/后台档案管理员登录 `/admin/fonds`）
  - 无关联全宗 → 点「删除」→ 确认 → 列表移除、审计有 `delete_fonds` 记录。
  - 有关联全宗 → 点「删除」→ 确认框提示「关联 N 件档案 / M 个档案盒…将被清空」→ 确认 → 全宗消失；回到档案列表确认这些档案的「全宗」字段已为空。
  - 「停用」按钮仍正常（只改 status）。

- [ ] **Step 3: commit**

```bash
git add backend/src/main/java/com/archive/service/FondsService.java \
        backend/src/main/java/com/archive/controller/FondsController.java \
        backend/src/test/java/com/archive/service/FondsServiceTest.java \
        frontend/src/views/admin/fonds/index.vue
git commit -m "feat(fonds): 新增全宗物理删除（关联 fonds_id 置空）"
```

## Phase 2：库房物理删除（无活动档案盒校验）

### Task 2.1：`WarehouseService.deleteRoom`（TDD）

**Files:**
- Modify: `backend/src/main/java/com/archive/service/WarehouseService.java`（在 `updateRoom` 方法后追加 `deleteRoom`）
- Test: `backend/src/test/java/com/archive/service/WarehouseServiceTest.java`（若不存在则新建；下面给出 setup + 3 个用例）

> `WarehouseService` 依赖 9 个字段（`@RequiredArgsConstructor` 构造注入）：`warehouseRoomMapper / storageLocationMapper / archiveBoxMapper / archiveBoxItemMapper / archiveMapper / jdbcTemplate / boxNoUtil / auditService`。下面 setup 按此顺序构造。

- [ ] **Step 1: 写失败测试**（若 `WarehouseServiceTest` 已存在，仅追加 3 个 `@Test` 并复用其 `@BeforeEach`；否则新建文件，import 包含 `org.junit.jupiter.api.{BeforeEach,Test}`、`static org.assertj.core.api.Assertions.assertThatThrownBy`、`static org.mockito.ArgumentMatchers.*`、`static org.mockito.Mockito.*`、`org.springframework.jdbc.core.JdbcTemplate`、`com.archive.exception.BusinessException`、`com.archive.common.ErrorCode`、各 Mapper/Util/AuditService/实体）

```java
@BeforeEach
void setup() {
    warehouseRoomMapper = mock(WarehouseRoomMapper.class);
    storageLocationMapper = mock(StorageLocationMapper.class);
    archiveBoxMapper = mock(ArchiveBoxMapper.class);
    archiveBoxItemMapper = mock(ArchiveBoxItemMapper.class);
    archiveMapper = mock(ArchiveMapper.class);
    jdbcTemplate = mock(JdbcTemplate.class);
    boxNoUtil = mock(BoxNoUtil.class);
    auditService = mock(AuditService.class);
    service = new WarehouseService(warehouseRoomMapper, storageLocationMapper,
            archiveBoxMapper, archiveBoxItemMapper, archiveMapper,
            jdbcTemplate, boxNoUtil, auditService);
}

@Test
void deleteRoom_有活动档案盒_抛CONFLICT且不删() {
    WarehouseRoom room = new WarehouseRoom();
    room.setId(11L);
    when(warehouseRoomMapper.selectById(11L)).thenReturn(room);
    when(jdbcTemplate.queryForObject(anyString(), eq(Long.class), eq(11L))).thenReturn(3L);

    assertThatThrownBy(() -> service.deleteRoom(11L))
            .isInstanceOf(BusinessException.class)
            .extracting("errorCode").isEqualTo(ErrorCode.BUSINESS_CONFLICT);
    verify(warehouseRoomMapper, never()).deleteById(anyLong());
}

@Test
void deleteRoom_无活动盒_清架位并物理删除() {
    WarehouseRoom room = new WarehouseRoom();
    room.setId(10L);
    room.setRoomNo("K001");
    room.setRoomName("1号库房");
    when(warehouseRoomMapper.selectById(10L)).thenReturn(room);
    when(jdbcTemplate.queryForObject(anyString(), eq(Long.class), eq(10L))).thenReturn(0L);

    service.deleteRoom(10L);

    verify(jdbcTemplate).update(eq("DELETE FROM storage_locations WHERE room_id = ?"), eq(10L));
    verify(warehouseRoomMapper).deleteById(10L);
    verify(auditService).log(eq("M07"), eq("delete_room"), eq("warehouse_room"), eq(10L), any());
}

@Test
void deleteRoom_库房不存在_抛NOT_FOUND() {
    when(warehouseRoomMapper.selectById(99L)).thenReturn(null);
    assertThatThrownBy(() -> service.deleteRoom(99L))
            .isInstanceOf(BusinessException.class)
            .extracting("errorCode").isEqualTo(ErrorCode.NOT_FOUND);
}
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd backend && mvn test -Dtest=WarehouseServiceTest`
Expected: 编译失败——`deleteRoom` 方法不存在。

- [ ] **Step 3: 实现 `deleteRoom`**（追加到 `WarehouseService.java`，紧跟 `updateRoom` 之后。`BusinessException`/`ErrorCode`/`Map`/`countOccupiedSlots` 均已在文件内可用）

```java
/** 16.10 删除库房（物理删除；存在活动档案盒时拒绝）。 */
@Transactional
public void deleteRoom(Long roomId) {
    WarehouseRoom room = warehouseRoomMapper.selectById(roomId);
    if (room == null) {
        throw new BusinessException(ErrorCode.NOT_FOUND, "库房不存在");
    }
    int activeBoxes = countOccupiedSlots(roomId);
    if (activeBoxes > 0) {
        throw new BusinessException(ErrorCode.BUSINESS_CONFLICT,
                "该库房存在 " + activeBoxes + " 个活动档案盒，无法删除，请先移出档案盒");
    }
    // 残留历史盒（moved/destroyed）解除架位引用，保留盒记录
    jdbcTemplate.update(
            "UPDATE archive_boxes SET location_id = NULL WHERE location_id IN " +
            "(SELECT id FROM storage_locations WHERE room_id = ?)", roomId);
    // 物理删除该库房全部架位（建库房时批量生成，无业务数据）
    jdbcTemplate.update("DELETE FROM storage_locations WHERE room_id = ?", roomId);
    // 物理删除库房
    warehouseRoomMapper.deleteById(roomId);
    auditService.log("M07", "delete_room", "warehouse_room", roomId,
            Map.of("roomNo", room.getRoomNo(), "roomName", room.getRoomName()));
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd backend && mvn test -Dtest=WarehouseServiceTest`
Expected: PASS。

### Task 2.2：`WarehouseController` DELETE 端点

**Files:**
- Modify: `backend/src/main/java/com/archive/controller/WarehouseController.java`（追加 `deleteRoom` 端点）

> 权限：沿用 `WarehouseController` 现有风格——该控制器现有端点均无显式 service 守卫，依赖 `/api/admin` 前缀的全局 admin 拦截。删除端点同样不加显式守卫。如需收紧到馆领导/系统管理员，可在 `WarehouseController` 内仿 `FondsController.requireBackOrAdmin()` 增加并调用（本次不做，保持一致）。

- [ ] **Step 1: 追加端点**（在 `updateRoom` 方法后。`DeleteMapping`/`R` 需 import，若未引入则补 `org.springframework.web.bind.annotation.DeleteMapping`）

```java
@DeleteMapping("/rooms/{roomId}")
public R<Void> deleteRoom(@PathVariable Long roomId) {
    warehouseService.deleteRoom(roomId);
    return R.ok();
}
```

- [ ] **Step 2: 编译校验**

Run: `cd backend && mvn -q compile`
Expected: 编译通过。

### Task 2.3：前端库房删除

**Files:**
- Modify: `frontend/src/api/warehouse.ts`（追加 `deleteWarehouseRoom`）
- Modify: `frontend/src/views/admin/warehouse/components/RoomList.vue`（外层 `button`→`div`、加删除按钮、emit `delete`、补样式）
- Modify: `frontend/src/views/admin/warehouse/index.vue`（`@delete` 监听、import、`onDeleteRoom`）

- [ ] **Step 1: `warehouse.ts` 追加 API**（在 `updateWarehouseRoom` 之后）

```ts
/** 删除库房（§16.10，无活动档案盒时允许） */
export function deleteWarehouseRoom(roomId: number): Promise<void> {
  if (USE_MOCK) {
    return import('@/mock/modules/warehouse').then((m) => m.mockDeleteWarehouseRoom(roomId))
  }
  return request.delete(`/admin/warehouse/rooms/${roomId}`)
}
```

> 联调阶段 `VITE_USE_MOCK=false`，走真实端点。若 mock 缺 `mockDeleteWarehouseRoom`，联调期可暂不补（真实接口已生效）；补 mock 仅在需要前端纯 mock 演示时。

- [ ] **Step 2: 改造 `RoomList.vue`**——外层 `<button>` 改 `<div role="button">`（HTML 不允许 button 内嵌 button），状态旁加删除按钮，emit 增 `delete`

将 `<template>` 内的 `<button ...>...</button>`（原 `RoomList.vue:9-27`）整体替换为：

```html
      <div
        v-for="r in rooms"
        :key="r.id"
        class="room-item"
        role="button"
        tabindex="0"
        :class="{ active: modelValue === r.id }"
        @click="emit('update:modelValue', r.id)"
      >
        <span class="room-head">
          <span>
            <strong>{{ r.roomNo }} {{ r.roomName }}</strong><br>
            <span class="muted">{{ r.rackCount }} 个机架 · {{ r.layersPerRack }} 层 · 每层 {{ r.boxesPerLayer }} 盒位</span>
          </span>
          <span class="room-head-right">
            <span class="status" :class="r.warning ? 'warning' : 'success'">{{ r.warning ? '容量告警' : '启用' }}</span>
            <el-button size="small" type="danger" plain @click.stop="emit('delete', r.id)">删除</el-button>
          </span>
        </span>
        <span class="usage">
          <span class="usage-track"><span class="usage-fill" :class="{ warning: r.warning }" :style="{ width: Math.round(r.occupancyRate * 100) + '%' }"></span></span>
          <span class="usage-text"><span>已用 {{ r.occupiedSlots }} / {{ r.capacity }}</span><span>{{ Math.round(r.occupancyRate * 100) }}%</span></span>
        </span>
      </div>
```

`<script setup>` 的 emit 增 `delete`（替换原 `defineEmits` 行）：

```ts
const emit = defineEmits<{
  (e: 'update:modelValue', id: number): void
  (e: 'delete', id: number): void
}>()
```

`<style scoped>` 补一行（加在 `.room-head` 规则附近）：

```css
.room-head-right { display: flex; align-items: center; gap: 8px; }
```

- [ ] **Step 3: `index.vue` 监听 `@delete` 并实现删除**——`<RoomList>` 加监听（替换 `index.vue:19`）

```html
      <RoomList :rooms="rooms" v-model="selectedRoomId" @delete="onDeleteRoom" />
```

import 增补（替换 `index.vue:57` 与 `:59` 两行）：

```ts
import { ElMessage, ElMessageBox } from 'element-plus'
import { createWarehouseRoom, deleteWarehouseRoom, getArchiveBoxes, getStorageLocations, getWarehouseRooms } from '@/api/warehouse'
```

新增 `onDeleteRoom`（紧跟 `onCreateRoom` 之后，约 `index.vue:148`）：

```ts
async function onDeleteRoom(roomId: number) {
  try {
    await ElMessageBox.confirm(
      '删除库房不可恢复。仅当库房内无活动档案盒时方可删除；历史档案盒记录会保留但解除架位归属。确认删除？',
      '删除库房',
      { type: 'warning', confirmButtonText: '删除', cancelButtonText: '取消' },
    )
  } catch {
    return // 用户取消
  }
  try {
    await deleteWarehouseRoom(roomId)
    ElMessage.success('库房已删除')
    if (selectedRoomId.value === roomId) selectedRoomId.value = null
    await loadRooms()
  } catch (e) {
    ElMessage.error(e instanceof Error ? e.message : '删除失败')
  }
}
```

### Task 2.4：浏览器验证 + commit

- [ ] **Step 1: 浏览器验证库房删除闭环**（admin 登录 `/admin/warehouse`）
  - 空库房（新建后未放盒）→ 「删除」→ 确认 → 列表移除、架位随之消失、审计有 `delete_room`。
  - 含活动档案盒的库房 → 「删除」→ 后端返回 409，前端弹「该库房存在 N 个活动档案盒…」。
  - 含已移动/销毁历史盒的库房 → 「删除」→ 成功；到档案盒列表确认这些盒记录仍在、但其架位/库房归属已为空。
  - 删除当前选中库房后，`selectedRoomId` 置空、架位区清空，无 JS 报错。

- [ ] **Step 2: commit**

```bash
git add backend/src/main/java/com/archive/service/WarehouseService.java \
        backend/src/main/java/com/archive/controller/WarehouseController.java \
        backend/src/test/java/com/archive/service/WarehouseServiceTest.java \
        frontend/src/api/warehouse.ts \
        frontend/src/views/admin/warehouse/components/RoomList.vue \
        frontend/src/views/admin/warehouse/index.vue
git commit -m "feat(warehouse): 新增库房物理删除（无活动档案盒校验）"
```

## Phase 3：跨单位纸质借阅凭证由档案馆代章

> 测试边界说明：`PdfGenerator` 输出 `byte[]`，项目无 PDFBox 等文本断言依赖，对 PDF 正文做单测无意义（违反测试诚信）。本 Phase 的正确性由「`BorrowServiceTest` 签名一致性回归 + 接口导出 PDF 视觉核对文案」保证，不为 PDF 渲染层强写单测。

### Task 3.1：`PdfGenerator` 改签名 + 盖章文案分支

**Files:**
- Modify: `backend/src/main/java/com/archive/util/PdfGenerator.java`（`generateBorrowVoucherPdf` 签名 + 第 222-224 段文案）

- [ ] **Step 1: 扩展方法签名**（替换 `PdfGenerator.java:163-164`）

```java
    public byte[] generateBorrowVoucherPdf(BorrowRequest borrow, Archive archive,
                                           User borrower, Organization org,
                                           boolean crossOrg, String archiveOrgName) {
```

- [ ] **Step 2: 改「单位意见（盖章）」段为跨单位分支**（替换 `PdfGenerator.java:222-224` 三行）

```java
            // 单位意见盖章区（跨单位纸质借阅由档案馆代原单位盖章）
            if (crossOrg) {
                String orgDisplay = (archiveOrgName != null && !archiveOrgName.isBlank())
                        ? archiveOrgName : "原属单位";
                doc.add(new Paragraph("档案馆代原属单位（" + orgDisplay + "）意见（档案馆盖章）：", headerFont));
                doc.add(new Paragraph("\n\n\n（档案馆代原属单位意见与盖章位置）\n\n\n", normalFont));
            } else {
                doc.add(new Paragraph("单位意见（盖章）：", headerFont));
                doc.add(new Paragraph("\n\n\n（借阅人所在单位意见与盖章位置）\n\n\n", normalFont));
            }
```

> 「档案馆归还确认（盖章）」段（原 226-228）保持不变。

- [ ] **Step 3: 编译校验**（此时 `BorrowService.exportVoucher` 仍用旧 4 参调用，会编译失败——预期，Task 3.2 修复）

Run: `cd backend && mvn -q compile`
Expected: 编译失败于 `BorrowService.exportVoucher` 对 `generateBorrowVoucherPdf` 的旧签名调用。

### Task 3.2：`BorrowService.exportVoucher` 识别跨单位并传参

**Files:**
- Modify: `backend/src/main/java/com/archive/service/BorrowService.java`（`exportVoucher` 末尾的 `return pdfGenerator.generateBorrowVoucherPdf(...)` 改造）
- Test: `backend/src/test/java/com/archive/service/BorrowServiceTest.java`（若其中 stub 了 `pdfGenerator.generateBorrowVoucherPdf`，需同步成新签名）

- [ ] **Step 1: 改造 `exportVoucher` 的 `return` 段**（替换 `BorrowService.java:210-213` 四行）

```java
        Archive archive = archiveMapper.selectById(b.getArchiveId());
        User borrower = userMapper.selectById(b.getBorrowerId());
        Organization org = (borrower != null && borrower.getOrganizationId() != null)
                ? organizationMapper.selectById(borrower.getOrganizationId()) : null;
        // 跨单位纸质借阅：档案所属单位 ≠ 借阅人单位 → 凭证改由档案馆代原单位盖章
        boolean crossOrg = archive != null && archive.getOrganizationId() != null
                && borrower != null && borrower.getOrganizationId() != null
                && !archive.getOrganizationId().equals(borrower.getOrganizationId());
        String archiveOrgName = null;
        if (crossOrg) {
            Organization archiveOrg = organizationMapper.selectById(archive.getOrganizationId());
            archiveOrgName = archiveOrg != null ? archiveOrg.getOrgName() : null;
        }
        return pdfGenerator.generateBorrowVoucherPdf(b, archive, borrower, org, crossOrg, archiveOrgName);
```

- [ ] **Step 2: 同步 `BorrowServiceTest` 的 stub 签名**

`BorrowServiceTest` 第 **288 行与 309 行**各有一处 `when(pdfGenerator.generateBorrowVoucherPdf(any(), any(), any(), any()))`（旧 4 参），把这两处都改成 6 参版本：

```java
        when(pdfGenerator.generateBorrowVoucherPdf(any(), any(), any(), any(), anyBoolean(), any()))
                .thenReturn(new byte[]{1, 2, 3});
```

若该测试类未 stub 此方法（依赖默认返回 `null`/不验证 PDF），则无需改动。

- [ ] **Step 3: 运行 `BorrowServiceTest` 确认不破**

Run: `cd backend && mvn test -Dtest=BorrowServiceTest`
Expected: PASS（编译通过、原有用例全绿）。

### Task 3.3：接口 + 视觉验证 PDF 文案 + commit

- [ ] **Step 1: 构造两条借阅数据并导出凭证对比**（API 或浏览器，用 `li.reader` 等内部查阅者账号）
  - **本单位纸质借阅**：借阅 1 件 `organization_id = 借阅人单位` 的纸质档案，审批通过后导出凭证 PDF → 打开核对：「单位意见（盖章）：」「（借阅人所在单位意见与盖章位置）」（文案不变）。
  - **跨单位纸质借阅**：借阅 1 件 `organization_id ≠ 借阅人单位` 的纸质档案（可临时把某档案 organizationId 改到他单位，或用 `dataScope=all` 账号发起），导出凭证 PDF → 核对：「档案馆代原属单位（{原属单位名}）意见（档案馆盖章）：」「（档案馆代原属单位意见与盖章位置）」。
  - 两张 PDF 的「档案馆归还确认（盖章）」段均保持不变。

- [ ] **Step 2: commit**

```bash
git add backend/src/main/java/com/archive/util/PdfGenerator.java \
        backend/src/main/java/com/archive/service/BorrowService.java \
        backend/src/test/java/com/archive/service/BorrowServiceTest.java
git commit -m "feat(borrow): 跨单位纸质借阅凭证由档案馆代原单位盖章"
```

## Phase 4：内部查阅者检索分级（最高风险，最后做）

> 适用对象（精确）：仅 `dataScope == own_org`（即内部查阅者）的检索生效新规则。`own_fonds`、`all`（馆领导/系统管理员/前后台档案管理员）保持原状。公众/移交走独立检索路径，不涉及。改 `applyDataScope` 一处，`internalSearch`（列表）与 `internalDetail`（详情）同步生效，不会出现「列表可见、详情 403」。

### Task 4.1：`SearchService.applyDataScope` 混合规则（TDD）

**Files:**
- Modify: `backend/src/main/java/com/archive/service/SearchService.java`（`applyDataScope` 的 `own_org` 分支 + import `SecurityLevel`）
- Test: `backend/src/test/java/com/archive/service/SearchServiceSearchTest.java`（追加 2 个用例，复用其现有 `@BeforeEach` 的 `service` 实例）

> `buildInternalWrapper` 是 package-private，`SearchServiceSearchTest` 同包（`com.archive.service`）可直接调。该方法纯构造 `QueryWrapper`、不查库，故无需为这两个用例额外准备 mapper 数据。

- [ ] **Step 1: 写失败测试**（追加到 `SearchServiceSearchTest.java`。import 补 `com.archive.dto.request.ArchiveSearchQuery`、`com.archive.enums.DataScope`、`com.archive.entity.Archive`、`com.baomidou.mybatisplus.core.conditions.query.QueryWrapper`，按文件现有情况取舍）

```java
@Test
void buildInternalWrapper_own_org_含本单位与他单位非密的OR分支() {
    ArchiveSearchQuery q = new ArchiveSearchQuery();
    QueryWrapper<Archive> w = service.buildInternalWrapper(q, 2, DataScope.own_org, 100L);
    String sql = w.getSqlSegment().toUpperCase();
    // 改造后 own_org 引入 (organization_id=? OR (organization_id<>? AND security_level=?)) 结构
    assertThat(sql).contains("ORGANIZATION_ID");
    assertThat(sql).contains("SECURITY_LEVEL");
    assertThat(sql).contains("OR");
}

@Test
void buildInternalWrapper_all_不施加组织过滤() {
    ArchiveSearchQuery q = new ArchiveSearchQuery();
    QueryWrapper<Archive> w = service.buildInternalWrapper(q, 4, DataScope.all, 100L);
    String sql = w.getSqlSegment().toUpperCase();
    assertThat(sql).doesNotContain("ORGANIZATION_ID");
}
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd backend && mvn test -Dtest=SearchServiceSearchTest`
Expected: `own_org` 用例 FAIL（当前 SQL 只含 `organization_id = ?`，无 `OR`/跨单位 `security_level` 分支）；`all` 用例 PASS（本就不过滤组织）。

- [ ] **Step 3: 改造 `applyDataScope` 的 `own_org` 分支**（替换 `SearchService.java:143-150` 整个方法）

```java
    /** 内部数据范围过滤（不按 open_status 硬过滤）。 */
    private void applyDataScope(QueryWrapper<Archive> w, DataScope scope, Long organizationId) {
        if (scope == DataScope.all || organizationId == null) return;
        if (scope == DataScope.own_org) {
            // 内部查阅者：本单位（受外层 security_level<=maxSL 约束）+ 其他单位仅非密
            w.and(inner -> inner
                    .eq("organization_id", organizationId)
                    .or(o -> o.ne("organization_id", organizationId)
                              .eq("security_level", SecurityLevel.NONE.getLevel())));
        } else if (scope == DataScope.own_fonds) {
            w.inSql("fonds_id", "SELECT id FROM fonds WHERE organization_id = " + organizationId);
        }
    }
```

并补 import（`SearchService.java` import 区）：`import com.archive.enums.SecurityLevel;`

> 生成 SQL 形态：`... AND security_level <= maxSL AND (organization_id = ? OR (organization_id <> ? AND security_level = 0))`。本单位受外层 `<=maxSL` 约束；其他单位额外要求 `=0`（非密）。

- [ ] **Step 4: 运行测试确认通过**

Run: `cd backend && mvn test -Dtest=SearchServiceSearchTest`
Expected: PASS（2 个新用例 + 原有用例全绿）。

### Task 4.2：e2e / 角色回归 + commit

- [ ] **Step 1: 跑后端全量单测，确认无回归**

Run: `cd backend && mvn test`
Expected: 全绿。重点关注 `SearchService*Test`、`BorrowServiceTest`。

- [ ] **Step 2: 跑 e2e 全量（Playwright）**

Run: `cd doc/e2e-tests && npm test`
Expected: 绝大多数用例通过。本次改动是「扩展」own_org 可见性（其他单位非密变可见），本单位原有可见档案不变，故多数 e2e 不受影响。**若有失败**：分析是否因某条 e2e 原本断言「内部查阅者看不到某跨单位档案」——若该档案恰好是非密，按新规则它应可见，需调整该用例的测试数据（把档案改为秘密/机密）或断言。不要为新规则削弱断言去掩盖问题，仅修正与新规则不符的旧假设。

- [ ] **Step 3: 角色/业务闭环手工抽验（浏览器，`li.reader` 内部查阅者）**
  - 本单位秘密档案：仍可见（≤maxSL=1 时看本单位内部、看不到本单位秘密？按 maxSL=1，本单位仅 ≤1=内部可见；若需验证「本单位全部密级」需用 maxSL 更高的内部账号）。
  - 其他单位非密档案：**新可见**（此前 own_org 看不到）。
  - 其他单位秘密/机密档案：**仍不可见**。
  - 馆领导（`fang.director`，dataScope=all）：全量可见，不受新规则影响。

- [ ] **Step 4: commit**

```bash
git add backend/src/main/java/com/archive/service/SearchService.java \
        backend/src/test/java/com/archive/service/SearchServiceSearchTest.java
git commit -m "feat(search): 内部查阅者检索分级（本单位按密级上限+他单位仅非密）"
```

---

## Phase 5：文档同步

### Task 5.1：更新业务/接口文档

**Files:**
- Modify: `doc/业务场景.md`
- Modify: `doc/接口文档.md`

- [ ] **Step 1: `doc/业务场景.md` 增补三处**
  - 内部查阅者检索规则：把原「按密级上限 + 所属单位/全宗范围」的笼统描述，细化为「`own_org` 内部查阅者：本单位档案按其密级上限 + 其他单位档案仅非密；`all` 管理角色不受限」。标注与 §5.2/11.2 一致。
  - 跨单位纸质借阅：补充「借阅其他单位纸质档案时，凭证由档案馆代原单位盖章（凭证文案标注原属单位名）」。
  - 库房/全宗删除：补充「库房在无活动档案盒时可物理删除（历史盒记录保留、解除架位归属）；全宗可物理删除，关联档案/档案盒的 fonds_id 置空」。

- [ ] **Step 2: `doc/接口文档.md` 增补**
  - 新增 `DELETE /api/admin/fonds/{fondsId}`：权限 back_archivist/sys_admin；物理删除；关联 archives/archive_boxes 的 fonds_id 置空；返回 `R<Void>`。
  - 新增 `DELETE /api/admin/warehouse/rooms/{roomId}`：权限 admin（依赖 `/api/admin` 前缀）；有活动档案盒时 409；物理删除库房及架位、历史盒 location 置空；返回 `R<Void>`。
  - 在借阅凭证导出（`GET /api/internal/borrow-requests/{id}/voucher`）说明处补充：跨单位纸质借阅（档案所属单位 ≠ 借阅人单位）时，凭证「单位意见」段文案为「档案馆代原属单位（{原属单位名}）意见（档案馆盖章）」。

- [ ] **Step 3: commit**

```bash
git add doc/业务场景.md doc/接口文档.md
git commit -m "docs: 同步跨单位代章与库房全宗删除的业务/接口说明"
```

---

## 验收清单（全部 Phase 完成后）

- [ ] 全宗删除：无关联直接删 / 有关联提示后置空删除 / 不存在 404，前端确认框文案正确。
- [ ] 库房删除：无活动盒可删 / 有活动盒 409 / 历史盒 location 置空，前端确认框文案正确。
- [ ] 凭证代章：本单位借阅凭证文案不变 / 跨单位借阅凭证含「档案馆代原属单位（X）意见」。
- [ ] 检索分级：own_org 可见他单位非密、不可见他单位秘密；all 角色不受影响；列表与详情一致。
- [ ] 后端 `mvn test` 全绿；e2e `npm test` 全绿（或已合理修正与新规则不符的旧断言）。
- [ ] 5 条 commit 信息符合 `doc/commit-convention.md`。
